{
    "context": "$dir",
    "dockerfile": "Dockerfile",
    "output": ["type=docker,dest=$dest"],
    "platforms": ["$platform"],
    "tags": ["$image"],
    "pull": true,
    "cache-from": ["$s3_cache"],
    "cache-to": ["$s3_cache"]
}
